name: Main Workflow

on:
  push:
    branches:
      - stage
  workflow_dispatch:

jobs:
  login:
    uses: ./.github/workflows/Login-Azure-Container-Registry-v2.yml
    with:
      login_server: manishkr.azurecr.io

